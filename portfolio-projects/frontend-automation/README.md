# Frontend automation

The runnable sample is maintained in [`../../qa-the-internet`](../../qa-the-internet). This folder owns its portfolio scope and skill instructions without duplicating the suite.

It currently provides 15 pytest and Playwright browser checks, a Docker runner, setup instructions, design rationale, and a green-run screenshot linked from the site.
