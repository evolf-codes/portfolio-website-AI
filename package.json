{
  "name": "portfolio-website-ai",
  "private": true,
  "description": "Root wrapper so Cloudflare Workers Builds can deploy from repo root.",
  "scripts": {
    "verify": "npm run verify:samples && npm run lint --prefix frontend && npm test --prefix frontend && npm run build --prefix frontend && npm run test:e2e --prefix frontend",
    "verify:samples": "sh scripts/verify_samples.sh",
    "verify:live": "portfolio-projects/frontend-automation/qa-the-internet/.venv/bin/python -m pytest portfolio-projects/frontend-automation/qa-the-internet/tests -q",
    "deploy": "npm ci --prefix frontend && npm run deploy --prefix frontend",
    "release": "npm run verify && npm run deploy"
  }
}
